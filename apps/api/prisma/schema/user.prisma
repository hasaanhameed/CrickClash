model User {
  id                     String                   @id @default(uuid())
  username               String                   @unique
  password               String
  streak                 Int                      @default(0)
  lastChallengeDate      DateTime?                @db.Date
  createdAt              DateTime                 @default(now())
  dailyChallengeAttempts DailyChallengeAttempt[]
}